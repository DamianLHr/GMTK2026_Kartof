using UnityEngine;

public class PuzzleInitializer : MonoBehaviour
{
    // script that handles the integral puzzle seuquence as well as all distractions, setting the randomness of both the strings as well as 
    // the chosen distractions
    // has information on the difficulty and sets it to the timer and uses it to pick the distractions and such


}
